# Scenes

Playable scenes (such as the submarine scene and the main scene) should be saved inside this folder.
'Component' scenes (such as enemies and items which will be instantiated) should be saved inside the
`prefabs` folder.
